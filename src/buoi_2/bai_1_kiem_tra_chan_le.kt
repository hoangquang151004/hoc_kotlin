package buoi_2

//Khai báo một biến số nguyên x và gán cho nó một giá trị bất kỳ.
//Dùng câu lệnh if-else (hoặc if dưới dạng biểu thức) để kiểm tra xem x là số chẵn hay số lẻ.
fun main(args: Array<String>) {
    var x = 23
    if (x%2 == 0){
        println("x la so chan")
    }
    else{
        println("x la so le")
    }
}
